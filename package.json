{
  "name": "pretty-env",
  "version": "0.0.1",
  "description": "CLI tool to display env information in an easy-to-read format.",
  "scripts": {
    "build":"tsc -p .",
    "dev": "node -r @swc-node/register src/main.ts",
    "lint": "deno lint -c deno.jsonc",
    "fmt": "deno fmt -c deno.jsonc"
  },
  "devDependencies": {
    "@swc-node/register": "^1.4.2",
    "@types/node": "^17.0.24",
    "typescript": "^4.6.3"
  }
}
